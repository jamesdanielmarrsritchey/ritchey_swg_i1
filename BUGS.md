# Bugs

- The search/browse feature keyword match percent display doesn't load the slider to the current value on page load. It only updates once the user interacts with it.
- The search/browse entries sometimes aren't loading correctly, causing duplicate pages of entries.

NOTES:
- This list may not be complete.