# To Do List

A list of things to potentially do in future releases.

HIGH PRIORITY:
- 

MEDIUM PRIORITY:
- Add javascript support for converting file preview text that has custom markup to HTML. The javascript doesn't have to perform the conversion. It can be pre-written into the page using PHP, and Javascript just decides which version to display. "Enhanced View (Any Ritchey Markup Language v4 markup will be interpretted visually)".
- Explicitly specify more of the colours within the default themes so that inversion, and shifting colours works better. Currently some colours aren't shifted, or inverted, because they aren't defined.
- Create a resources bash script which automatically generates thumbnails for image, and video uploads using ffmpeg.

LOW PRIORITY:
- Add manual tag support, and auto tag support. The auto tag would be a PHP function which searches the name of each entry, and checks to see if any of those terms are in a list of tags and subtags, and if so, assigns those tags.
- Remove unused dependencies, and unused custom dependencies from the project. At the very least, move them to the resources folder.
- Replace lingering legacy UUIDs in PHP scripts.
- Update all custom dependencies to use current naming scheme.
- Add support for custom checksum type.
- Add support for audio file uploads that do previews to display a thumbnail if it exists, as cover art.
- Expand assets collection to have more images that are useful for blog posts.
- Add support for an alternate theme the user can switch to/from. This would be useful for giving the user light/dark mode options, if the site wishes to provide them.