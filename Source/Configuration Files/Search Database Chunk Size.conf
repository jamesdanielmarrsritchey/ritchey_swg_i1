Name: Search Database Chunk Size
Description: Specifies a quantity limit for each HTML file that is part of the search, and browse page sets. Default value is "50", which means each file contains up to 50 database entries. A larger value may reduce how often users hit single item pages when filtering, but makes pages less reader friendly when nothing filters out.
Content: 8